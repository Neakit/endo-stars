export enum Roles {
  Admin = "ADMIN",
  Supervisor = "SUPERVISOR",
  Manager = "MANAGER",
}

export enum Layouts {
  Default = "DEFAULT",
  Auth = "AUTH",
}

export enum AUTH_TOKEN {
  ACCESS_TOKEN = "ACCESS_TOKEN",
  REFRESH_TOKEN = "REFRESH_TOKEN",
}

export enum BUTTON_PROCCESS_STATE {
  READY = "READY",
  LOADING = "LOADING",
  DONE = "DONE",
}
